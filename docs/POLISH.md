# Polish — deferred tuning

Correct-but-not-final values and rough edges intentionally deferred so they don't block progress. Move
an item here instead of leaving a silent `// TODO`, and link the doc that owns it.

> Forward-looking backlog. The server is now substantially built — it plays the full FTUE loop end-to-end
> (boot → onboarding → forest dungeon → equip loot → store buy → witch dungeon → skill unlock; see
> [progression-loop.md](gameplay/progression-loop.md)) — so polish items can now be collected here as they surface.
> Likely first entries: balance constants pulled from `game-data/loose/GameplaySettings` (and the decrypted
> `game-data/settings-extracted/` spec DB) that we serve before the full catalog wiring is finalised.
